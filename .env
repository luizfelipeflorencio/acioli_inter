# Database Configuration
# These settings are used by both the Docker containers and the PHP application

# MySQL container configuration
DB_HOST=mysql
DB_USER=acioli_user
DB_PASSWORD=acioli_password
DB_NAME=acioli_climatizacao
DB_ROOT_PASSWORD=root_password
DB_PORT=3307

# PHP Configuration
PHP_PORT=8000